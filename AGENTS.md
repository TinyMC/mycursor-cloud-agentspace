# AGENTS.md

## Cursor Cloud specific instructions

This is a blank test repository (`mycursor-cloud-agentspace`) with no application code, services, or dependencies. The repository currently contains only a `README.md`.

- **No build system, package manager, or dependencies** are present.
- **No services** need to be started.
- **No tests or lint checks** exist to run.
- The update script is intentionally a no-op (`echo "No dependencies to install"`).

When application code is added to this repository, update this file and the VM update script accordingly.
